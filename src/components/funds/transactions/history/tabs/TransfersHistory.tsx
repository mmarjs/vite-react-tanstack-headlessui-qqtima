import { Table } from '~/components/table/Table';
import { Tab } from '~/components/tabs/Tabs';

export const TransfersHistory = () => {
	return (
		<Tab>
			<Table name="transfersHistory" rows={[]} />
		</Tab>
	);
};
